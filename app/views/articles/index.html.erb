
<h1 class="text-5xl mb-2">Awesome Articles!</h1>
<p class="text-xl mb-8"> Where the best news of the internet comes to die</p>
 
<table>
  <tr>
    <th>Title</th>
    <th>Content</th>
    <th colspan="3"></th>
  </tr>
 
  <% @articles.each do |article| %>
    <tr>
      <td><%= article.title %></td>
      <td><%= article.content %></td>
      <td><%= link_to 'Show', article_path(article) %></td>
      <td><%= link_to 'Edit', edit_article_path(article) %></td>
      <td><%= link_to 'Delete', article_path(article),
              method: :delete,
              data: { confirm: 'Are you sure you want to delete this article?' } %></td>
    </tr>
  <% end %>
</table>

<%= link_to 'New Article', new_article_path, class: "bg-teal-500 hover:bg-teal-700 text-white font-bold py-4 px-8 rounded" %>